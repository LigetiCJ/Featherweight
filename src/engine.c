#include "stateMachine.h"
/*  initialization
registries
These contain declarations of all item, entity, and tile containers (NOTE_item, NOTE_entity, NOTE_tile)

All containers are saved to a list 


*/

typedef struct{
    char *itemName;
    unsigned int *texture;

}NOTE_item;